// Minimaler Mock für @rork/toolkit-sdk
module.exports = {
  // füge hier die Methoden hinzu, die dein Code importiert/aufruft
  // Beispiel:
  init: () => {},
  track: () => {},
  // default-Export, falls irgendwo `import sdk from '@rork/toolkit-sdk'` benutzt wird
  default: {
    init: () => {},
    track: () => {},
  },
};
